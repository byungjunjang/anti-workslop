# 파이프라인을 쓰는 데 필요한 것. 공개본에도 그대로 실린다.
# 클론한 뒤 한 번: pip install -r requirements.txt
beautifulsoup4>=4.14.2   # styleguide-builder/scripts/collect.py 본문 추출
lxml>=6.0.2              # 같은 파일의 파서 선택. 없으면 html.parser 로 떨어져 문장 수가 달라진다
