# {{p:display}} 글쓰기 문체 가이드라인

{{p:author}}({{p:display}})의 {{p:site}} 공개 글 {{v:n_posts}}편({{v:date_from}}~{{v:date_to}}, 약 {{v:n_sentences_hund}}문장)을 분석해 정리한 문체 기준이다. 에이전트가 이 문체로 새 글을 쓰거나, 기존 글을 이 문체로 고치거나, 결과를 검수할 때 이 문서 하나만 읽고 작업할 수 있게 만들었다.

- 버전: {{v:version}} · 코퍼스 `{{v:corpus_sha8}}` · 도구 styleguide-builder {{v:skill_version}}
- 이 문서의 수치는 코퍼스에서 관찰한 경향이다. 문장마다 맞추는 할당량이 아니라 글 전체의 분포로 맞춘다. 근거 통계는 `styleguides/{{p:slug}}/stats.md`, 검사 명령은 `check_style.py --kit styleguides/{{p:slug}} 초고.md`.
- 본문의 예문은 문체를 설명하는 자료다. 예문 속 사실·이력·숫자를 새 글의 사실로 가져오지 않는다.

---

## 0. 에이전트가 이 문서를 읽는 법

### 규칙 등급

| 표시 | 뜻 | 어길 때 |
|---|---|---|
| **[hard]** | 이 문체의 필수 조건 | 하나라도 어기면 이 문체가 아니다. 반드시 고친다. |
| **[soft]** | 경향. 글 전체에서 대체로 맞추면 된다 | 두세 개 어긋나도 된다. 원문 의미를 해치면서 맞추지 않는다. |
| (참고) | 관찰 수치·예문 | 판단 근거로만 쓴다. 복제 대상이 아니다. |

### 지시가 충돌할 때의 우선순위

1. 이번 작업에 대한 사용자의 명시적 지시 (목적·형식·분량·보존 범위·수정 허용 범위)
2. 확인된 사실, 원문의 의미, 출처, 보호할 내용 (§10 보존 규칙)
3. 매체와 독자의 관례 (공식 양식, 필수 항목, 길이 제한, 용어 표준)
4. 이 문서의 [hard] 규칙
5. 이 문서의 [soft] 규칙
6. 관찰 수치와 예문

상위 조건을 어기면서 하위 규칙을 맞추지 않는다. 사용자 지시 두 개가 양립할 수 없거나, 사실과 다른 내용을 사실처럼 쓰라는 요청이면 충돌 지점을 밝히고 확인한다. 낮은 우선순위의 문체 규칙 때문에 이미 충족된 요청을 바꾸지 않는다.

### 세 가지 대원칙

- **없는 것을 지어내지 않는다.** 경험·성과·숫자·대화·감정을 문체를 위해 만들지 않는다.
- **이미 맞는 문장은 건드리지 않는다.** 바꿀 이유를 이 문서의 규칙 번호로 댈 수 없으면 수정하지 않는다.
- **수치는 글 전체로 맞춘다.** 문장 하나를 {{t:len_mean.rec}}자로 만드는 게 아니라 평균과 분포를 본다.

---

## 1. 작업 시작: 종류·입력·적용 범위 확정

### 1-1. 작업 종류

| 종류 | 요청 해석 | 할 일 | 기본 변경 범위 |
|---|---|---|---|
| 새 글 작성 | 주제·메모·자료만 주고 "작성해줘" | 제공된 자료·독자를 바탕으로 쓴다 | 구성과 문장을 새로 만든다. 사실과 경험은 자료에만 근거한다 |
| 문체 변환·편집 | 원문과 함께 "다듬어줘", "이 문체로 바꿔줘" | 내용을 유지하고 표현·호흡만 고친다 | 규칙에 어긋난 문장만 고친다. 전면 재구성은 별도 요청이 있을 때만 |
| 검수 | "검토해줘", "맞는지 봐줘" | §11 점검표로 판정한다 | 검수만 요청했다면 수정하지 않는다. 수정까지 허용했다면 발견된 문제만 고친다 |

이미 한 번 변환한 결과를 다시 받으면 새 글로 재생성하지 않고 기존 글로 검수한다(§10 멱등성).

### 1-2. 입력과 기본값

매번 질문할 목록이 아니다. 요청과 원문에서 확인되는 값을 먼저 쓰고, 빠진 항목만 기본값으로 처리한다.

| 입력 | 확인할 것 | 빠졌을 때 |
|---|---|---|
| 주제·목적 | 무엇을 설명하고 독자가 무엇을 얻는가 | 원문에서 명확하면 유지. 핵심이 없으면 그 부분만 질문 |
| 독자·매체·분량 | 배경지식, 블로그/뉴스레터/SNS/보고서, 길이 | 기존 글은 원래 형식·분량 유지. 새 글은 이 저자의 기본 장르인 블로그 설명문({{len:default}}자) |
| 사실·경험·숫자 | 근거 자료, 화자의 실제 경험, 확정한 주장 | 없는 경험·성과·숫자는 넣지 않는다. 자리표시자로 남긴다(§10) |
| 화자 | 누구의 목소리인가 | 지정이 없으면 "직접 해본 실무자"라는 위치만 가져온다. {{p:author}}의 이름·이력·성과·경험은 화자가 {{p:author}} 본인으로 확인될 때만 실제 자료로 쓴다 |
| 보호할 내용 | 수치, 이름, 인용, 링크, 고정 문구, 유지할 문단 | §10 보존 규칙을 기본 적용 |
| 추가 지시 | 특정 어조, 금지 표현, 필수 항목, 출력 형식 | 이번 작업의 구체적 지시로 반영 |

자료에 모순이 있거나 핵심 주장을 뒷받침할 정보가 없으면 임의로 메우지 않는다. 결정에 필요한 질문만 하고, 별도로 진행할 수 있는 부분은 계속한다. 단순한 취향이나 형식의 누락은 기본값으로 처리한다.

### 1-3. 장르·길이별 적용 범위

| 글의 종류 | 유지할 것 | 조정할 것 |
|---|---|---|
| 블로그·뉴스레터·방법론 안내 ({{len:valid}}자) | 이 문서 전체. 수치 목표가 유효한 길이 | — |
| 짧은 글 (SNS·공지·댓글, 800자 이하) | 문장·종결·접속·쉼표 규칙(§3~§5)은 그대로 | 구조(§6)는 "훅 → 사례 한 문단 → 권유 한 문장"으로 줄인다. '느낀 점'·CTA 생략. 비율 통계(§11-1)는 평가하지 않는다 |
| 이메일·회고·근황 | 명료한 판단, 자연스러운 연결, 존댓말 | 긴 배경·번호 소제목·CTA를 강제하지 않는다. 회고는 교훈이나 감사로 끝낼 수 있다 |
| 보고서·제안서·공식 문서 | 근거·조건·적용 범위의 명확성 | 기관 양식과 요구된 종결형을 따른다. 괄호 사족·구어·자기 고백·이모티콘·CTA를 넣지 않는다 |
| 다른 분야의 해설·리뷰 | 개념을 상황과 효과로 풀어 설명하는 방식, 문장·종결·접속 규칙 | §7-1 주제 어휘만 해당 분야로 바꾼다. {{p:topic_words|·}} 같은 원래 소재 어휘를 억지로 넣지 않는다 |
| 창작·서정적인 글 | 요청에 맞는 명료함과 화자의 일관성 | 이 분석의 근거가 약한 영역이다. 구조·어미 비율을 강제하지 않는다 |

경험·질문·비유·대조·괄호·행동 권유는 모두 선택 요소다. 없다는 이유만으로 추가하지 않는다.

---

## 2. 목소리 정의

**{{q:voice_one_liner}}**

{{q:voice_bullets}}

작성 원칙 다섯 가지:

{{q:writing_principles}}

화자의 실제 경험이 제공되지 않았다면 확인된 사례·개념 설명·독자의 질문으로 시작한다. 가상 사례는 실제 경험과 구분해 표시한다.

---

## 3. 문장 길이와 호흡

### 관찰 수치와 목표

{{table:3}}

(참고: 짧은·긴 문장은 길이 기준이며 문법적 단문·복문 구분이 아니다.)

### 규칙

1. **중간 길이 설명문을 기본으로 이어 붙인다.** 긴 문장 다음에는 다시 중간 길이로 돌아온다. 상황·조건·이유가 밀접하면 한 문장 안에서 설명해도 좋다.
2. **한 문장에 중심 판단은 하나.** 서로 다른 주장을 여러 개 넣어 중심이 흐려졌다면 나눈다.
3. **긴 문장은 나열로 만든다.** [hard] 종속절을 겹치지 말고 "(1) …하고, (2) …하고, (3) …하는" 대등 연결이나 "A하고, B하고, C합니다"로 늘린다. "~하는 ~한 ~에 대한 ~" 관형절 사슬은 끊어서 다음 문장으로 넘긴다.
   - {{q:long_sentence_example}}
4. **짧은 문장은 역할이 있을 때만 쓴다.** 역할은 다음과 같다.
{{q:short_roles}}
5. **단문을 연달아 쌓지 않는다.** [hard] 짧은 문장 두 개가 붙으면 하나를 앞 문장에 '-고,'로 합친다. "짧다. 강하다. 그래서 통한다." 식의 리듬은 이 문체가 아니다.
6. **문단은 주장으로 열고 근거·조건절로 닫는다.** [soft] 마지막 문장에 {{q:para_closing_endings}}처럼 근거절·조건절을 붙여 가장 길게 두는 편이다(문단 첫 문장 평균 {{int:para_first_len}}자, 끝 문장 {{int:para_last_len}}자). 다만 긴 문장 뒤에 반드시 짧은 문장을 붙이는 식으로 순서를 고정하지 않는다. 설명의 의미가 전환되는 위치에서 호흡을 바꾼다.
7. **같은 논점을 설명하는 문장은 한 문단으로 묶는다.** 한 문장짜리 강조 문단은 허용하되 남발하지 않는다. 한 문단에 한 문장씩 나열하는 AI 특유의 배치는 피한다.

### 리듬의 예

{{q:rhythm_quote}}

{{q:rhythm_examples}}

숫자 배열을 복제할 필요는 없다. **설명이 충분히 쌓인 지점에서 짧게 확인하거나 독자를 안심시키는 기능**을 가져온다.

---

## 4. 접속부사, 쉼표, 괄호, 부호

### 4-1. 접속부사

{{q:conj_role_table}}

(참고 빈도, 문장 어디든: {{conjfreq:8}}. {{q:conj_note}})

- 문두 접속부사는 **{{one_in:pct_conj_start}}문장에 1개** 수준({{int:pct_conj_start}}%, 허용 {{t:pct_conj_start.minmax}}%). [{{t:conj_consecutive.level}}] 두 문장 연속으로 접속부사로 시작하지 않는다(접속부사 연속 {{t:conj_consecutive.range}}).
- 접속어는 앞뒤 관계를 먼저 확인하고 고른다. 연결이 이미 분명하면 생략한다. 횟수를 맞추려고 넣거나, 사건이 순서대로 일어났다는 이유만으로 인과 접속어를 붙이지 않는다.
{{q:conj_patterns}}
- **접속부사 뒤에 쉼표를 찍지 않는다.** [hard] 예외는 '{{p:conj_comma_allowed|·}}' 뿐이다.

### 4-2. 쉼표

{{table:4_2}}

- 쉼표는 세 곳에 쓴다: ① 병렬 나열(직무, 도구, 고객 유형, 실행 단계) ② '-고,' 대등 연결 ③ 조건·배경을 먼저 말하고 주된 판단으로 넘어가는 곳. ①②가 전체 쉼표의 {{fraction:pct_list_or_go_comma}}이어야 한다. [{{t:pct_list_or_go_comma.level}}]
- 주어 뒤, 접속부사 뒤, 부사구 뒤에는 찍지 않는다.
- 화자의 짧은 덧말을 문장 중간에 끼울 때는 쉼표 대신 **괄호**로 뺀다. {{q:comma_insert_exception}}
- 쉼표가 여러 개 필요하면 문장을 나눌지 먼저 본다. 평균을 맞추려고 쉼표를 넣거나 빼지 않는다.
- 병렬 압축은 **슬래시**: {{q:slash_examples}}

### 4-3. 괄호

전체 문단의 약 {{int:pct_para_with_paren}}%에 괄호가 나온다. 용도는 다음과 같다.

{{q:paren_uses}}

괄호를 지워도 본문의 주장이 이해되어야 한다. 중요한 근거나 필수 조건을 괄호 안에 숨기지 않는다. 친근함을 만들려고 농담이나 자기비하를 억지로 넣지 않는다.

### 4-4. 그 밖의 부호

{{q:punct_table}}

---

## 5. 종결어미, 어조, 인칭

### 5-1. 비율 목표

{{table:5_1}}

### 5-2. 규칙

1. **기본 톤은 합쇼체 고정.** [hard] 반말 본문은 없다. 사용자가 다른 종결형이나 공식 양식을 지정하면 그것을 우선한다.
2. **문단 끝 한 문장만 해요체로 내린다.** '-죠·-거든요·-잖아요'를 연속으로 쓰지 않는다. 대화적인 느낌은 어미보다 경험을 밝히는 방식과 독자에게 말을 거는 위치에서 만든다.
{{q:haeyo_examples}}
3. **문장의 기능에 따라 종결을 고른다.**

{{q:ending_function_table}}

4. **한 섹션의 종결 시퀀스:** 사실(-입니다) → 판단(-라고 생각합니다) → 전망(-것입니다) → 권유(-세요/-바랍니다). 단정과 완화를 문장 단위로 교대한다.
5. **확인된 사실까지 추측형으로 바꾸지 않는다.** '생각합니다'는 판단임을 밝힐 때만 쓴다. 모든 문장 끝에 같은 표현을 붙이지 않는다.
6. **소제목과 본문을 구분한다.** 소제목은 '-다'로 짧고 단정하게 써도 된다({{q:heading_da_example}}). 본문은 존댓말로 풀어간다. 소제목의 단정적 말투를 글 전체에 적용하지 않는다.

### 5-3. 인칭과 호칭

- 저자 자신: **저 / 제**. 문두 '저는'은 {{one_in:pct_first_person_author_start}}문장에 1개 정도.
- 독자와 공유하는 일반 1인칭: **내 / 나**. 문장의 {{int:pct_generic_first_person}}%에 나오며, 한 문장에 '저'와 '내'가 같이 있어도 된다.
  - {{q:mixed_person_example}}
- 독자 호칭: {{q:reader_address}} [hard: 여러분·당신 합산 {{t:yeoreobun_dangsin.max}}회 이하]

---

## 6. 글의 뼈대와 정보 배열

### 6-1. 시작 (첫 문단)

넷 중 하나로 연다. 첫 문단 안에 이 글이 무엇을 다루는지 드러나야 한다. 단, "이 글에서는 ~를 다룹니다/알아보겠습니다" 같은 메타 문장으로 알리지 않는다.

| 도입 | 관찰 | 쓰기 좋은 상황 | 예 |
|---|---|---|---|
| 자기 이력·경험·근황·실제 대화 | {{count:post_structure.도입=이력·경험}}편 | 판단의 출발점이 되는 실제 경험이 있다 | {{q:opening_examples_experience}} |
| 질문 | {{count:post_structure.도입=질문}}편 | 독자가 이미 품고 있을 구체적 고민이 있다 | {{q:opening_examples_question}} |
| 정의·배경 | {{count:post_structure.도입=정의·배경}}편 | 독자가 개념이나 문제를 먼저 이해해야 한다 | {{q:opening_examples_definition}} |
| 결론·주장 | {{count:post_structure.도입=결론·주장}}편 | 제안이 간단하고 이유를 곧바로 설명할 수 있다 | {{q:opening_examples_claim}} |

경험담을 먼저 검토하되 모든 글을 개인 경험으로 시작하지 않는다. 배경으로 열 때는 도입에 필요한 배경만 쓰고 늘어지지 않는다.

### 6-2. 본문 순서 — 방법론·설득 글의 기본형

{{q:body_order}}

### 6-3. 변형 — 경험담 글

**내가 겪은 상황 → 발견한 문제 → 해본 방법 → 결과와 해석 → 독자가 해볼 일**

| 단계 | 담을 내용 |
|---|---|
| 상황 | 어떤 역할과 조건에서 무엇을 하고 있었는가 |
| 문제 | 무엇이 막혔고, 기존 방식은 왜 충분하지 않았는가 |
| 방법 | 무엇을 선택했고 실제로 어떻게 실행했는가 |
| 결과와 해석 | 어떤 반응을 확인했고 그 경험에서 무엇을 배웠는가 |
| 독자의 실행 | 비슷한 상황의 독자는 어디서부터 시작할 수 있는가 |

### 6-4. 소제목

- 명사구 {{int:heading_forms.noun_pct}}%, 평서 '-다' {{int:heading_forms.da_pct}}%, 청유·명령 {{int:heading_forms.imperative_pct}}%, 의문 {{int:heading_forms.question_pct}}% 비율로 섞는다(표제 {{n:heading_forms.total}}개 기준). [soft]
- 번호는 "1." "(1)" "01." 중 하나로 글 안에서 통일한다.
- 주장을 소제목에 그대로 올린다: {{q:heading_examples}}

### 6-5. 끝 (마지막 문단)

- **요약으로 끝내지 않는다.** [hard] '요약하면/정리하면/결론적으로/마지막으로'로 시작하는 문단을 만들지 않는다.
- 마지막 문장은 **권유·낙관 전망·감사·달라진 관점의 회고** 중 하나다. [{{t:closing_is_call_or_outlook.level}}] 실천을 돕는 글은 권유가 기본이고, 근황·회고 글은 감사나 교훈으로 끝낼 수 있다. 모든 글에 행동 촉구나 홍보를 넣지는 않는다.
{{q:closing_examples}}

### 6-6. 질문과 경험의 위치

- 질문은 **글 첫 문장이나 섹션 첫 문장에만**. 본문 중간에서는 자문자답으로 바로 닫는다: {{q:question_position_example}}
- 개인 경험은 **주장 바로 다음 문단**에. 섹션마다 "저도/저 역시/저의 경우에는" 한 문단.
- 실패와 숫자를 숨기지 않는다: {{q:failure_examples}}

---

## 7. 어휘, 표기, 습관 표현, 감정, 대조, 예시

### 7-1. 쓰는 어휘 (주제가 바뀌면 첫 세 줄만 교체)

{{q:lexicon_table}}

(참고 빈도: {{lexfreq:7}}.)

- 전문어를 소개했다면 돈·시간·고객 반응·실행 순서 등 독자가 이해할 수 있는 결과로 풀어준다.
- 같은 대상은 핵심 명사를 반복해도 좋다. 반복을 피하려고 동의어를 바꾸다 개념이 달라 보이게 하지 않는다.
- 구어 표현은 독자와 매체에 맞을 때만 쓴다. 원래 소재({{p:topic_words|·}}) 어휘를 다른 주제에 억지로 넣지 않는다.

### 7-2. 표기 규칙 (주제가 바뀌어도 유지)

{{table:7_2}}

- 개념어 첫 등장에 **괄호 영문 병기**: {{q:gloss_examples}} 괄호 앞 띄어쓰기는 통일하지 않아도 된다.
- 툴·서비스 이름은 원어 그대로: {{q:tool_names}}
- **띄어쓰기와 오탈자:** 새 글은 표준 띄어쓰기로 쓴다. 기존 글을 편집할 때는 교정 허용이 없으면 원문의 띄어쓰기가 섞여 있어도 둔다. 어느 경우에도 **없던 오탈자를 만들지 않는다.** [hard] 원문의 오탈자는 교정을 안 거친 흔적이지 규칙이 아니다.

### 7-3. 습관 표현 (억지로 피하지 않는다)

- "~을/를 통해(서)": {{one_in:pct_tonghae}}문장에 1개.
{{q:habit_expressions}}

### 7-4. 감정·감각 표현

- 감정은 경험과 판단에 붙여 짧게 드러낸다: {{q:emotion_examples}} 이런 표현은 원문에 있으므로 금지하지 않는다.
- 장면을 그리는 감각 묘사(색·소리·촉감·풍경·냄새)는 쓰지 않는다. [{{t:sensory_total.level}}] 반짝이는, 부드러운, 따뜻한, 눈부신, 설레는, 아름다운.
- 감정을 형용사로 꾸미기보다 "~을 느꼈습니다", "~마저 듭니다"처럼 서술어로 보고한다. [soft]

### 7-5. 대조 구문

'A가 아니라 B' / 'A보다는 B' / 'A 말고 B'를 쓴다. 문장의 {{int:pct_contrast}}% 안팎, {{t:pct_contrast.max}}% 이하. [{{t:pct_contrast.level}}]

{{q:contrast_pairs}}
- 대조의 역할: 경험으로 이해가 달라진 지점, 비슷해 보이는 전략의 우선순위, 독자가 오해할 수 있는 주장의 범위 교정.
- 쓰지 않는 것: [hard] 추상 대구("본질이 아니라 현상"), 3연 대구("A도 아니고 B도 아니고 C다"), "단순한 X를 넘어 Y". 문장을 강해 보이게 하려고 매 문단 같은 구문을 반복하지 않는다.

### 7-6. 예시 드는 방법

'예를 들어'라는 표지 없이 본문에 바로 박는다. [hard: '예를 들어' 류 {{t:ye_reul_deureo.max}}회 이하] 숫자가 든 문장은 {{one_in:pct_numeric_sent}}문장에 1개.

{{q:example_methods}}

---

## 8. 하지 않는 것

| 유형 | 내용 | 등급 |
|---|---|---|
| 장면 감각 묘사 | 색·소리·촉감·풍경·냄새를 그리는 형용사 | {{t:sensory_total.level}} |
| 문학적 수사 | 의인화, 자연·예술 비유, 시적 표현 | soft |
| 결론 요약 문단 | '요약하면', '정리하면', '결론적으로', '마지막으로'로 시작 | hard |
| 금지 접속·조사 | 한편, 그러므로, 게다가, 더불어, 아울러, ~에 있어, ~함으로써 | {{t:banned_total.level}} (합산 {{t:banned_total.max}}회 이하) |
| 쉼표 남용 | 접속부사 뒤 쉼표 / 주어 뒤 쉼표 | {{t:conj_comma_bad.level}} / {{t:comma_after_subject.level}} |
| 단문 연타 | "짧다. 강하다. 그래서 통한다." | hard |
| 종속절 중첩 | "~하는 ~한 ~에 대한 ~" 관형절 사슬 | hard |
| 추상 대구·3연 대구 | "본질이 아니라 현상", "A도 B도 아니고 C" | hard |
| 열린 질문으로 끝내기 | 물음표는 도입에만 | hard |
| 호칭 반복 | '여러분', '당신' 합산 {{t:yeoreobun_dangsin.max}}회 초과 (§5-3) | {{t:yeoreobun_dangsin.level}} |
| 일부러 오탈자 | 업계 표기는 유지하되 없던 오탈자 생성 | hard |
| AI 티 나는 틀 | 불릿 안에 볼드 소제목, 이모지로 시작하는 목록, "핵심 포인트:", "이 글에서는 ~를 다룹니다", "~에 대해 알아보겠습니다", "중요한 것은 ~입니다", 한 문단에 한 문장씩, '혁신적인'·'효과적인' 같은 평가 형용사로 사례를 대체하기 | hard |
| 지어낸 근거 | 없는 경험·성과·숫자·대화·감정 | hard |
| 선택 요소 강제 | 경험·질문·비유·대조·괄호·CTA를 "없다는 이유로" 추가 | hard |
| AI 티 (공용 목록) | 줄표 남용, 'X: Y' 극적 콜론, 잠언 마무리, 출처 불명 권위, 억지 3항, 용어 변주, 대화 잔재, 직역 어휘. 이 표는 이 문체 고유 금지만 다루고 공용 목록·등급은 `principles/ai-tells-ko.md` | hard(S1) / soft(S2·S3) |

---

## 9. 변환 예시 (전형적인 AI 문장 → 이 문체)

{{q:conversion_examples}}

**바꾼 것:** {{q:conversion_commentary}}

위 예문의 사례·이력·숫자는 문체 설명용이며 새 글의 사실로 가져오지 않는다. **변환할 원문에 숫자가 없으면 §10에 따라 자리표시자를 남긴다.**

---

## 10. 보존·멱등성 규칙 (편집·변환·검수 시)

문체 변환은 재집필이 아니다. 같은 요청·자료·가이드 버전으로 두 번 적용하면 결과가 같아야 한다.

anti-workslop 윤문은 이 절의 보존 규칙을 `principles/invariants.md`의 불변식 넷으로 검수한다.

### 10-1. 보존할 것

- 주장, 사실, 수치·단위·날짜, 인명·기관명·제품명, 링크·출처, 인용과 인용 표시, 코드, 수식, 표 데이터, 지정된 고정 문구, 문단 순서, 소제목의 뜻. 금액 단위나 날짜 형식도 요청 없이 바꾸지 않는다. 오타처럼 보여도 명시적 수정 허용 없이는 고치지 않는다.
- 부정·조건·시제·확신의 정도·주장 범위. '일부'를 '모두'로, '가능할 수 있다'를 '가능하다'로, 계획을 완료 사실로 바꾸지 않는다.
- 원문에 없는 인과·비교·평가를 접속어나 어휘 선택으로 추가하지 않는다.
- 화자와 독자, 인용한 사람의 구분. {{p:author}}의 이름·경력·성과·경험을 다른 화자의 정체성으로 가져오지 않는다.
- 이미 적합한 제목·문단·목록·문장. 새 예시·질문·권유·괄호·소제목·홍보 문구를 그럴듯하다는 이유로 추가하지 않는다.

문장을 합치거나 나눌 수는 있어도 정보를 더하거나 빼지 않는다.

### 10-2. 없는 것을 지어내지 않는다

원문에 사례나 숫자가 없으면 `[사례 필요: …]`, `[숫자 필요: …]` 자리표시자를 남긴다. 저자만 채울 수 있는 칸이다. 완성본을 요청받았다면 자리표시자를 본문에 두는 대신 미확정 지점을 본문 밖 메모로 알리거나, 범위를 좁힌 결과임을 밝힌다.

### 10-3. 멱등성 절차

1. 첫 적용에서 작업 종류·보호할 내용·우선할 지시를 확정하고, 반복 적용에서도 같은 기준을 유지한다.
2. 수정 전에 문제 위치·위반 규칙 번호·필요한 변경을 짧게 식별한다. 규칙 번호를 댈 수 없는 수정('더 자연스러울 것 같다')은 하지 않는다.
3. 문제가 있는 부분만 고친다. 한 문장을 고칠 때는 그 문단의 리듬만 본다. 이미 수정한 표현에 접속어·설명·자기 고백·권유를 다시 덧붙이지 않는다. 같은 뜻의 표현을 매번 다른 동의어로 바꾸지 않는다.
4. hard만 강제한다. soft는 원문 의미를 해치면서 맞추지 않는다.
5. 수정 후 보존 규칙과 사용자 요구를 재확인한다. 고친 부분이 새 오류를 만들었을 때만 그 부분을 재수정한다. 통계와 다르다는 이유로 재작성을 반복하지 않는다.
6. 해결할 문제가 없으면 **수정 없음**으로 종료한다. 파일 편집이면 본문·줄바꿈·인코딩을 유지하고 다시 저장하지 않는다. 대화로 반환하면 기존 본문을 그대로 반환하고 상태 설명을 본문 안에 넣지 않는다.
7. 원문이 이미 이 저자의 글이고 hard 규칙을 통과하면 변환 대상이 아니다.

사용자가 새 사실·독자·분량·목표를 제시했거나 가이드 버전이 바뀌면 새 조건으로 검수할 수 있다. 이때도 바뀐 조건과 관계없는 부분은 유지한다. 새 글 생성은 확률적이므로 같은 입력만으로 문구가 완전히 같아지는 것은 보장하지 않는다. 글자 단위로 동일한 결과가 필요하면 확정본과 적용 조건을 저장하고, 조건이 같을 때 확정본을 재사용한다.

### 10-4. 결과물의 형식

- 사용자가 지정한 파일명·형식·필수 항목을 따른다. 편집에서는 원문의 제목·소제목·목록 구조를 유지하며 요청 없이 늘리지 않는다.
- 완성 글에 검수 점수, 통계, 수정 이유, 버전 안내, '수정 없음' 같은 작업 상태를 섞지 않는다. 요청한 경우 별도 메모나 검수 기록으로 전달한다.
- 글쓰기 요청만으로 이 가이드나 통계를 갱신하지 않는다. 개별 글에 적용한 예외를 영구 규칙에 추가하지 않는다.

---

## 11. 점검표

각 항목은 **충족 / 해당 없음 / 수정 필요 / 정보 부족** 중 하나로 판정한다. '해당 없음'인 요소는 새로 넣지 않는다. '수정 필요'는 위치와 위반 규칙 번호를 함께 적는다. '정보 부족'은 임의 작성으로 해결하지 않는다. 800자 이하 글에서는 11-1의 비율 항목을 평가하지 않는다.

### 11-1. 자동 계측 가능 항목

hard 하나라도 실패하면 이 문체가 아니다. soft는 경향이므로 두세 개 어긋나도 된다. `python -X utf8 check_style.py --kit styleguides/{{p:slug}} 초고.md`가 이 표를 그대로 센다. 도구가 없으면 수동으로 판정한다.

{{table:11_1}}

### 11-2. 눈으로 보는 항목

- [ ] 사용자가 요청한 작업 종류·형식·분량·보존 범위에 맞는가?
- [ ] 수치·단위·고유명사·인용·링크와 부정·조건·시제·확신 수준을 보존했는가?
- [ ] 경험·성과·대화·감정을 사실에 맞게 썼는가? 없는 것은 자리표시자로 남겼는가?
- [ ] 직접 확인한 사실과 개인적 해석·기대를 구분했는가?
- [ ] 개인 경험이 독자의 문제와 연결되는가?
- [ ] 추천의 이유와 적용 조건을 설명했는가?
- [ ] 긴 문장은 나열 구조이고 중심 판단을 따라갈 수 있는가?
- [ ] 짧은 문장이 자문·단정·확인·전환·추임새 중 실제 역할을 하는가?
- [ ] 문단 끝 해요체가 한 문장뿐인가?
- [ ] 접속어와 쉼표가 실제 문장 관계를 드러내는가?
- [ ] 괄호를 지워도 본문이 이해되는가?
- [ ] 'A가 아니라 B'가 구체 명사쌍이고 뒤에 근거가 따라오는가?
- [ ] 비유가 원래 논점으로 돌아오는가?
- [ ] 시작이 이력·질문·정의·주장 중 하나이고 첫 문단에 글의 범위가 드러나는가?
- [ ] 마무리가 권유·전망·감사·회고 중 하나로 끝나는가?
- [ ] 관찰 비율을 맞추려고 자연스러운 문장을 바꾸지 않았는가?
- [ ] 이미 적합한 문장을 취향만으로 바꾸거나, 선택 요소를 의무적으로 추가하지 않았는가?
- [ ] 이전 적용 때 넣은 제목·설명·맺음말을 중복해서 추가하지 않았는가?
- [ ] 해결할 문제가 없으면 수정 없이 종료했는가?
{{q:visual_checklist_extra}}

---

## 12. 새 글 작성 순서

1. **독자와 문제를 한 문장으로 적는다.** 누가 어떤 고민을 해결하기 위해 읽는 글인가.
2. **근거를 모은다.** 실제 경험, 확인된 결과, 개인적 해석, 추가 확인 필요 정보를 구분한다. 없는 것은 자리표시자.
3. **도입을 고른다.** 이력·경험 / 질문 / 정의·배경 / 주장.
4. **뼈대를 세운다.** §6-2 기본형 또는 §6-3 경험담형. 짧은 글은 훅 → 사례 → 권유.
5. **내용을 먼저 완성한다.** 상황과 이유, 방법, 결과, 적용 조건이 자연스럽게 이어지게.
6. **호흡과 어조를 다듬는다.** 긴 문장은 나열로, 문단은 주장으로 열고 근거로 닫고, 문단 끝 한 문장만 해요체, 확인·안심·자문이 필요한 자리에만 단문.
7. **독자의 다음 판단·행동을 확인한다.** 무엇을 이해하고 어디서부터 시작하면 되는지.
8. **문체를 과장한 흔적을 지운다.** 억지 구어, 억지 농담, 반복 대조, 수치 맞추기용 접속어, AI 틀.
9. **§11로 검수한다.** hard 실패 항목만 고치고 재검수한다.

기존 글의 편집·검수에는 이 순서로 다시 쓰지 않고 §10을 쓴다.

---

## 13. 회귀 검수 예시

아래는 코퍼스의 추가 분석이 아니라 판단을 확인하기 위한 **교육용 가상 입력**이다. 새 글의 실제 경험으로 가져오지 않는다. 반복 적용 시에는 1차 결과를 입력으로 삼고 같은 조건을 유지한다.

| 상황과 입력 | 올바른 처리 | 다시 적용했을 때 |
|---|---|---|
| 짧은 공지: '회의는 9월 10일 오전 10시에 시작합니다. 참석 여부를 9월 8일까지 알려주세요.' | 날짜·시간·요청을 보존하고 그대로 둔다 | 질문·배경·소제목을 추가하지 않고 수정 없음으로 종료한다 |
| 공식 보고서의 고정 문구: '자료 검토 후 재논의 예정.' | 요청된 공식 종결형과 고정 문구를 유지한다 | '-습니다'로 바꾸지 않는다 |
| 근거 있는 대조: '이번 검토의 목적은 도구 교체가 아니라, 기존 절차에서 누락된 확인 단계를 찾는 것입니다.' | 설명할 차이가 있고 구체 명사쌍이므로 대조를 유지한다 | 대조 금지 규칙을 새로 만들어 삭제하지 않는다 |
| 조건과 불확실성: '일부 참가자에게 도움이 될 수 있습니다. 장기적인 효과는 확인하지 못했습니다.' | 일부·가능성·미확인 상태를 그대로 유지한다 | '모두에게 효과적입니다'로 강화하지 않는다 |
| 화자의 경험이 없는 주제 메모 | 확인된 개념·사례나 질문으로 시작한다. 사례 자리는 `[사례 필요: …]` | '제가 해보니' 같은 가짜 경험을 덧붙이지 않는다 |
| 두 출처가 서로 다른 수치를 제시하는 자료 | 충돌을 밝히고 필요한 확인을 요청한다 | 편집 과정에서 하나를 임의로 정답으로 바꾸지 않는다 |
| 띄어쓰기 교정도 허용된 설명문: '이 안내를 읽으면 신청 절차를 이해할수 있습니다.' | '…이해할 수 있습니다.'로 최소 수정한다 | 교정된 문장을 그대로 둔다 |
| 주제만 '산책 기록'으로 바뀐 글 | 해당 소재의 관찰과 어휘를 유지한다 | {{p:topic_words|·}} 같은 원래 코퍼스의 소재를 넣지 않는다 |
| AI 초안: '결론적으로, 여러분도 지금 시작해보세요!' | '결론적으로' 삭제, '여러분' 제거, 시대 진단 + 권유로 마무리 재구성 | 이미 고친 마무리에 CTA·감사를 또 덧붙이지 않는다 |
| 접속부사 뒤 쉼표: '그러나, 저는 다르게 생각합니다.' | '그러나 저는 다르게 생각합니다.'로 쉼표만 제거 | 문장 나머지를 건드리지 않는다 |

회귀 검수는 의미·보호 내용·불필요한 추가 여부를 확인하는 것이다. 예시와 다른 표현이라는 이유만으로 실패로 보지 않는다. 완전히 동일한 문구를 요구하는 경우에는 새로 생성하지 않고 확정본을 비교·재사용한다.

---

## 14. 압축 프롬프트 (시스템 프롬프트·CLAUDE.md·AGENTS.md에 붙이는 블록)

```
아래 규칙으로 한국어 글을 쓰거나 고쳐라. 우선순위: 사용자 지시 > 사실·의미 보존 > 매체 관례 > [hard] > [soft] > 수치·예문. 수치는 문장 단위가 아니라 글 전체 분포로 맞춘다.

[작업] 원문 없이 "작성해줘"면 새 글, 원문과 함께 "다듬어줘"면 문체 편집, "검토해줘"면 검수(수정 금지). 한 번 변환한 글을 다시 받으면 재생성하지 않고 검수한다. 800자 이하 글은 훅→사례→권유 3단계로 줄이고 비율 규칙은 평가하지 않는다. 공식 문서는 기관 양식과 종결형을 우선한다.

[보존] 주장·사실·숫자·고유명사·인용·링크·문단 순서·부정·조건·시제·확신 정도를 바꾸지 않는다. 사례나 숫자가 없으면 지어내지 말고 [사례 필요: …] [숫자 필요: …]로 남긴다. 규칙 번호를 댈 수 없는 수정은 하지 않는다. 같은 글에 두 번 적용하면 결과가 같아야 하고, 고칠 게 없으면 '수정 없음'으로 끝낸다. {{p:author}}의 이력·성과를 다른 화자에게 이식하지 않는다. 완성 글에 검수 점수·수정 이유·작업 상태를 섞지 않는다.

[목소리] {{q:voice_short}}

[문장] 평균 {{t:len_mean.rec}}자({{t:len_mean.minmax}}), 100자 초과 {{t:pct_xlong_gt100.max}}% 이하. 문단 {{t:sent_per_para_mean.rec}}문장 안팎. 주장으로 열고 근거·조건절로 닫는다. 긴 문장은 "(1) …, (2) …" 나열로만, 관형절 사슬은 끊어서 다음 문장으로 넘긴다. 단문은 역할이 있는 자리에만, 연달아 쓰지 않는다. 한 문단 한 문장 배치 금지.

[종결] 합쇼체 {{t:pct_hapsyo.rec}}%({{t:pct_hapsyo.minmax}}). 문단 끝 한 문장만 해요체, 해요체 두 문장 연속 금지. 판단은 "~라고 생각합니다"({{int:pct_saenggak}}%), 전망은 "~것입니다"({{int:pct_geot_ipnida}}%). 확인된 사실을 추측형으로 바꾸지 않는다. 본문 해라체 금지(소제목·인용 제외). 섹션 시퀀스: 사실→판단→전망→권유.

[접속] 문두 접속부사 {{one_in:pct_conj_start}}문장에 1개({{t:pct_conj_start.minmax}}%), 연속 금지, 연결이 분명하면 생략. {{q:conj_short}} 접속부사 뒤 쉼표 금지({{p:conj_comma_allowed|·}}만 예외). 쉼표는 나열과 '-고,'에만, 문장당 {{t:commas_per_sent.rec}}개({{t:commas_per_sent.minmax}}). 부연은 괄호, 병렬은 슬래시. 괄호를 지워도 본문이 이해되어야 한다.

[구조] {{q:structure_short}} 요약 문단으로 끝내지 않는다. 질문은 글·섹션 첫 문장에만.

[어휘] {{q:lexicon_short}} 저자는 '저/제', 일반 1인칭은 '내/나', 독자는 '~하시는 분'. 여러분/당신 합산 {{t:yeoreobun_dangsin.max}}회 이하. 새 글은 표준 띄어쓰기, 편집은 요청 없으면 교정 안 함, 오탈자 생성 금지.

[예시] '예를 들어' 없이 고유명사 나열, (ex. …), 내 숫자, 도구·일상 비유(비유 후 논점 복귀). 'A가 아니라 B'는 구체 명사쌍으로만, 뒤에 근거, {{int:pct_contrast}}% 안팎.

[감정] 경험에 붙는 짧은 감정어는 허용. 장면 감각 묘사(색·소리·풍경·냄새)는 금지.

[금지] 문학적 비유·의인화, 결론 요약, 한편·그러므로·게다가·더불어·아울러·~함으로써, 단문 연타, 종속절 중첩, 추상·3연 대구, 열린 질문 마무리, 불릿 안 볼드 소제목, 이모지 목록, "핵심 포인트", "~에 대해 알아보겠습니다", 평가 형용사('혁신적인')로 사례 대체, 없는 경험·숫자·커뮤니티·강의, 없는 요소를 '없다는 이유로' 추가.
[AI 티] 공용 목록(principles/ai-tells-ko.md) S1 0건: 줄표 남용, 극적 콜론, 잠언·요약 마무리(삭제하고 있는 구체 문장으로 끝), 출처 불명 권위, 억지 3항, 용어 변주, 대화 잔재, 직역 어휘(주입·봉인·축·가깝다).
```
